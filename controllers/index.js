const userController = require('./api/userController');
const thoughtController = require('./api/thoughtController');
const reactionController = require('./api/reactionController');

module.exports = {
  userController,
  thoughtController,
  reactionController,
};
